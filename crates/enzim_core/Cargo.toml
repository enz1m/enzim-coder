[package]
name = "enzim_core"
version = "0.1.0"
edition = "2024"
rust-version = "1.92"

[dependencies]
libc = "0.2"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde_json = "1.0"
